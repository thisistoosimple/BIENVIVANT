<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>LIS — Liste d'Attente</title>
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;1,400;1,500&family=Inter:wght@300;400;900&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --bg: #000000;
            --white: #f4f2ee;
            --grey-mid: #888;
            --grey-dark: #333;
            --accent: #c8b89a;
            --serif: 'EB Garamond', Georgia, serif;
            --sans: 'Inter', sans-serif;
        }

        html, body {
            height: 100%;
            background: var(--bg);
            color: var(--white);
            font-family: var(--sans);
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        /* ── NAV ── */
        nav {
            position: fixed;
            top: 0; left: 0; right: 0;
            z-index: 100;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 40px;
            height: 68px;
            mix-blend-mode: difference;
        }

        .logo-img {
            height: 20px;
            width: auto;
            filter: brightness(0) invert(1);
        }

        .nav-back {
            font-family: var(--sans);
            font-size: 10px;
            font-weight: 900;
            letter-spacing: 0.2em;
            color: white;
            text-decoration: none;
            text-transform: uppercase;
            transition: opacity 0.2s;
        }
        .nav-back:hover { opacity: 0.5; }

        /* ── HERO + FORM LAYOUT ── */
        .page {
            min-height: 100vh;
            display: grid;
            grid-template-columns: 1fr 1fr;
        }

        /* LEFT — image côté */
        .left {
            position: relative;
            overflow: hidden;
            background: #0a0a0a;
        }

        .left-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            filter: grayscale(30%) brightness(0.75);
            transform: scale(1.04);
            transition: transform 8s ease;
        }

        .left:hover .left-img {
            transform: scale(1.0);
        }

        /* Overlay gradient */
        .left::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to right, transparent 60%, #000 100%);
            pointer-events: none;
        }

        /* Collection tag */
        .collection-tag {
            position: absolute;
            bottom: 48px;
            left: 40px;
            z-index: 2;
        }
        .collection-tag span {
            font-family: var(--sans);
            font-size: 9px;
            font-weight: 900;
            letter-spacing: 0.3em;
            color: rgba(255,255,255,0.35);
            text-transform: uppercase;
            display: block;
            margin-bottom: 6px;
        }
        .collection-tag p {
            font-family: var(--serif);
            font-size: 2.2rem;
            font-style: italic;
            font-weight: 400;
            color: rgba(255,255,255,0.85);
            line-height: 1;
        }

        /* RIGHT — form côté */
        .right {
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 100px 64px 80px;
            position: relative;
        }

        /* Grain overlay subtil */
        .right::before {
            content: '';
            position: absolute;
            inset: 0;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
            opacity: 0.4;
            pointer-events: none;
        }

        /* Numéro décoratif */
        .deco-number {
            font-family: var(--serif);
            font-size: 9rem;
            font-weight: 400;
            font-style: italic;
            color: rgba(255,255,255,0.03);
            position: absolute;
            top: 48px;
            right: 48px;
            line-height: 1;
            user-select: none;
            pointer-events: none;
        }

        .form-eyebrow {
            font-family: var(--sans);
            font-size: 9px;
            font-weight: 900;
            letter-spacing: 0.3em;
            color: var(--accent);
            text-transform: uppercase;
            margin-bottom: 20px;
            opacity: 0;
            animation: fadeUp 0.7s 0.1s ease forwards;
        }

        .form-title {
            font-family: var(--serif);
            font-size: clamp(2.8rem, 5vw, 4.5rem);
            font-weight: 400;
            line-height: 1.0;
            margin-bottom: 10px;
            opacity: 0;
            animation: fadeUp 0.7s 0.2s ease forwards;
        }
        .form-title em {
            font-style: italic;
            color: var(--accent);
        }

        .form-subtitle {
            font-family: var(--serif);
            font-size: 1.05rem;
            font-style: italic;
            color: var(--grey-mid);
            line-height: 1.6;
            margin-bottom: 56px;
            max-width: 340px;
            opacity: 0;
            animation: fadeUp 0.7s 0.3s ease forwards;
        }

        /* ── FORM ── */
        .waitlist-form {
            display: flex;
            flex-direction: column;
            gap: 0;
            opacity: 0;
            animation: fadeUp 0.7s 0.4s ease forwards;
        }

        .input-group {
            position: relative;
            margin-bottom: 14px;
        }

        .input-label {
            display: block;
            font-family: var(--sans);
            font-size: 9px;
            font-weight: 900;
            letter-spacing: 0.25em;
            color: var(--grey-dark);
            text-transform: uppercase;
            margin-bottom: 10px;
            transition: color 0.2s;
        }

        .input-group:focus-within .input-label {
            color: var(--accent);
        }

        .input-field {
            width: 100%;
            background: transparent;
            border: none;
            border-bottom: 1px solid var(--grey-dark);
            color: var(--white);
            font-family: var(--serif);
            font-size: 1.1rem;
            padding: 10px 0 14px;
            outline: none;
            transition: border-color 0.3s ease;
            letter-spacing: 0.02em;
        }

        .input-field::placeholder {
            color: rgba(255,255,255,0.15);
            font-style: italic;
        }

        .input-field:focus {
            border-bottom-color: var(--accent);
        }

        /* Ligne animée sous l'input */
        .input-line {
            position: absolute;
            bottom: 0; left: 0;
            height: 1px;
            width: 0;
            background: var(--accent);
            transition: width 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .input-group:focus-within .input-line {
            width: 100%;
        }

        /* ── SUBMIT ── */
        .btn-submit {
            margin-top: 40px;
            width: 100%;
            padding: 18px 24px;
            background: var(--white);
            color: #000;
            border: none;
            font-family: var(--sans);
            font-size: 10px;
            font-weight: 900;
            letter-spacing: 0.28em;
            text-transform: uppercase;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: color 0.35s ease;
        }

        .btn-submit::before {
            content: '';
            position: absolute;
            inset: 0;
            background: #000;
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.45s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .btn-submit:hover { color: var(--white); }
        .btn-submit:hover::before {
            transform: scaleX(1);
            transform-origin: left;
        }

        .btn-submit span {
            position: relative;
            z-index: 1;
        }

        /* ── DISCLAIMER ── */
        .form-note {
            margin-top: 20px;
            font-family: var(--sans);
            font-size: 9px;
            letter-spacing: 0.12em;
            color: rgba(255,255,255,0.2);
            line-height: 1.6;
        }

        /* ── SUCCESS STATE ── */
        .success-state {
            display: none;
            flex-direction: column;
            align-items: flex-start;
            gap: 16px;
        }

        .success-check {
            width: 48px;
            height: 48px;
            border: 1px solid var(--accent);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent);
            font-size: 18px;
            animation: scaleIn 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .success-title {
            font-family: var(--serif);
            font-size: 2rem;
            font-style: italic;
            color: var(--white);
        }

        .success-body {
            font-family: var(--serif);
            font-size: 1rem;
            font-style: italic;
            color: var(--grey-mid);
            line-height: 1.7;
            max-width: 300px;
        }

        /* ── DIVIDER ── */
        .form-divider {
            display: flex;
            align-items: center;
            gap: 20px;
            margin: 48px 0 28px;
            opacity: 0;
            animation: fadeUp 0.7s 0.5s ease forwards;
        }
        .form-divider::before,
        .form-divider::after {
            content: '';
            flex: 1;
            height: 1px;
            background: rgba(255,255,255,0.06);
        }
        .form-divider span {
            font-family: var(--sans);
            font-size: 9px;
            font-weight: 900;
            letter-spacing: 0.22em;
            color: rgba(255,255,255,0.15);
            text-transform: uppercase;
        }

        /* ── COUNTER ── */
        .counter-row {
            display: flex;
            gap: 40px;
            opacity: 0;
            animation: fadeUp 0.7s 0.6s ease forwards;
        }
        .counter-item {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }
        .counter-num {
            font-family: var(--serif);
            font-size: 1.6rem;
            font-style: italic;
            font-weight: 400;
            color: var(--white);
        }
        .counter-lbl {
            font-family: var(--sans);
            font-size: 9px;
            font-weight: 900;
            letter-spacing: 0.2em;
            color: rgba(255,255,255,0.2);
            text-transform: uppercase;
        }

        /* ── ANIMATIONS ── */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to   { opacity: 1; transform: translateY(0); }
        }

        @keyframes scaleIn {
            from { transform: scale(0.7); opacity: 0; }
            to   { transform: scale(1);   opacity: 1; }
        }

        /* ── RESPONSIVE ── */
        @media (max-width: 900px) {
            .page { grid-template-columns: 1fr; }
            .left { height: 55vw; min-height: 280px; }
            .left::after {
                background: linear-gradient(to bottom, transparent 60%, #000 100%);
            }
            .right { padding: 48px 28px 72px; }
            nav { padding: 0 24px; }
            .deco-number { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">
            <img src="lis_logo.png" alt="LIS" class="logo-img">
        </div>
        <a href="index.html" class="nav-back">← Retour</a>
    </nav>

    <div class="page">

        <!-- LEFT -->
        <div class="left">
            <img src="mockup.jpg" alt="Collection Rêverie 1.0" class="left-img"
                 onload="this.style.opacity=1" style="opacity:0; transition: opacity 1s ease;">
            <div class="collection-tag">
                <span>Collection</span>
                <p>1.0 Rêverie</p>
            </div>
        </div>

        <!-- RIGHT -->
        <div class="right">
            <div class="deco-number">01</div>

            <p class="form-eyebrow">Accès Anticipé</p>
            <h1 class="form-title">Rejoignez<br>la <em>liste</em></h1>
            <p class="form-subtitle">Soyez les premiers informés du lancement officiel. Accès exclusif, avant tout le monde.</p>

            <!-- FORM -->
            <form class="waitlist-form" id="waitlistForm" onsubmit="handleSubmit(event)" novalidate>
                <div class="input-group">
                    <label class="input-label" for="email">Adresse e-mail</label>
                    <input
                        class="input-field"
                        type="email"
                        id="email"
                        name="email"
                        placeholder="votre@email.com"
                        autocomplete="email"
                        required
                    >
                    <div class="input-line"></div>
                </div>

                <button type="submit" class="btn-submit" id="submitBtn">
                    <span id="btnText">REJOINDRE LA LISTE</span>
                </button>

                <p class="form-note">Aucun spam. Désabonnement libre à tout moment.</p>
            </form>

            <!-- SUCCESS -->
            <div class="success-state" id="successState">
                <div class="success-check">✓</div>
                <p class="success-title">Vous êtes sur la liste.</p>
                <p class="success-body">Nous vous contacterons en avant-première lors du lancement de la collection Rêverie 1.0.</p>
            </div>

            <div class="form-divider"><span>Collection</span></div>

            <div class="counter-row">
                <div class="counter-item">
                    <span class="counter-num">2</span>
                    <span class="counter-lbl">Pièces</span>
                </div>
                <div class="counter-item">
                    <span class="counter-num">400</span>
                    <span class="counter-lbl">GSM</span>
                </div>
                <div class="counter-item">
                    <span class="counter-num">∞</span>
                    <span class="counter-lbl">Durabilité</span>
                </div>
            </div>
        </div>

    </div>

    <script>
        function handleSubmit(e) {
            e.preventDefault();
            const email = document.getElementById('email').value.trim();
            if (!email || !email.includes('@')) {
                document.getElementById('email').style.borderBottomColor = '#e05';
                return;
            }

            const btn = document.getElementById('submitBtn');
            const btnText = document.getElementById('btnText');

            // Loading state
            btn.disabled = true;
            btnText.textContent = '...';

            setTimeout(() => {
                // Show success
                document.getElementById('waitlistForm').style.display = 'none';
                const success = document.getElementById('successState');
                success.style.display = 'flex';
                // Animate children
                [...success.children].forEach((el, i) => {
                    el.style.opacity = '0';
                    el.style.transform = 'translateY(12px)';
                    el.style.transition = `all 0.5s ${i * 0.12}s ease`;
                    requestAnimationFrame(() => {
                        requestAnimationFrame(() => {
                            el.style.opacity = '1';
                            el.style.transform = 'translateY(0)';
                        });
                    });
                });
            }, 900);
        }
    </script>

</body>
</html>
